package ble.Entities;

public class EntityVariable extends Entity {	
	
	public EntityVariable(String name, String value, String scope) {
		super.setName(name);
		super.setValue(value);
		super.setScope(scope);
	}
		
}
